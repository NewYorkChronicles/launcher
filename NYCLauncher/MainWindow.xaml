<Window x:Class="NYCLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        Title="New York Chronicles"
        Width="1000" Height="700"
        MinWidth="1000" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        ShowInTaskbar="True"
        Opacity="0">
    <Grid>
        <Border x:Name="ClipBorder" Background="#060609" CornerRadius="14"/>
        <Grid>
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=ClipBorder}"/>
            </Grid.OpacityMask>
            <cef:ChromiumWebBrowser x:Name="Browser"/>
            <Rectangle Height="60" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,0,300,0" Fill="Transparent" MouseLeftButtonDown="DragBar_MouseDown" Panel.ZIndex="99"/>
        </Grid>
    </Grid>
</Window>
